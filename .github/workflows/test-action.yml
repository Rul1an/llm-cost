name: Test llm-cost action
on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: actions/checkout@v4
      - uses: Rul1an/llm-cost@v1
        with:
          budget: "100.00"
